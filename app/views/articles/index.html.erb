<p>A breaking news item</p>
<p>Some really breaking action</p>
<p>Learn Rails 5</p>
<p>Build awesome rails applications</p>

<% content_for :title, "Articles" %>
<%= link_to 'New Article', new_article_path %>

<% if @articles.any? %>
    <% @articles.each do |article| %>
        <div id="<%= dom_id(article) %>">
            <h1>
                <%= article.title %>
            </h1>
            <p> <%= article.content %> </p>

            <%= form_with url: article_comments_path(article), remote: false do |f| %>
                <%= f.label :comment %>
                <%= f.text_field :comment %>
                <%= f.label :name, 'Your name' %>
                <%= f.text_field :name %>
                <%= f.submit 'Send comment' %>
            <% end %>
        </div>
           
        
    <% end %>
<% else %>
    <h1>There are no articles in the database..</h1>
<% end %>
</div>